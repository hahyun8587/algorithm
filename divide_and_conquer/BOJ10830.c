#include <stdio.h>

#define MAX_SIZE 5
#define MODULOR 1000

void dot(int (*src1)[MAX_SIZE], int n, int m, int (*src2)[MAX_SIZE], int p, int o, int (*dst)[MAX_SIZE]);
void clone(int (*src)[MAX_SIZE], int n, int (*dst)[MAX_SIZE]);
void calc(int (*src1)[MAX_SIZE], int n, int (*src2)[MAX_SIZE], int (*dst)[MAX_SIZE]);
void mpow(int (*src)[MAX_SIZE], int n, int (*dst)[MAX_SIZE], int b);
void aprintf(int (*arr)[MAX_SIZE], int n);

int main() {



    return 0;
}

void clone(int (*src)[MAX_SIZE], int n, int (*dst)[MAX_SIZE]) {
        



}

