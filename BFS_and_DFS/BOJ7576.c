#include <stdio.h>
#include <malloc.h>

#define MAX_SIZE 1000

int BFS(int (*arr)[MAX_SIZE], int n, int m, int *s);
int minDate(int (*arr)[MAX_SIZE], int n, int m);

int main() {



    return 0;
}

int BFS(int (*Arr)[MAX_SIZE], int n, int m, int *s) {
    



}

